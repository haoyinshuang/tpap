KISSY.add(function (S, Node, Overlay, Button) {
    //适配代码在这
    function SafeOverlay(frameGroup) {      
    }
    return SafeOverlay;
}, {
    requires: ['core','node', 'overlay', 'button', 'overlay/1.0/index.js']
});
